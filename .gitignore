# Generated site (built in CI, never committed)
/site/

# Author's raw paper uploads — NEVER deployed. Everything here is ignored EXCEPT
# the README that documents the upload convention. (One zip was ~3.3 GB; GitHub
# rejects files >100 MB — never commit sources.)
/sources/*
!/sources/README.md
*.zip

# Python
.venv/
venv/
__pycache__/
*.pyc

# macOS / editor cruft
.DS_Store
__MACOSX/
Icon?
